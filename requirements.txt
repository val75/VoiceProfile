# VoiceProfile — production dependencies (pinned)
# Regenerate versions from a known-good venv; keep this list to direct deps only.

# Web framework + WSGI server
Flask==3.1.2
gunicorn==23.0.0

# i18n
Flask-Babel==4.0.0

# Database / ORM / migrations
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
alembic==1.16.5
psycopg2-binary==2.9.11

# Config
python-dotenv==1.1.1

# External services: Whisper STT (HTTP) and LLM extraction
requests==2.32.5
openai==2.6.1
json-repair==0.44.1

# Profile media + sharing
Pillow==11.3.0
qrcode[pil]==8.2
